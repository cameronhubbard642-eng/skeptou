{
  "name": "auth-core",
  "version": "0.1.0",
  "private": true,
  "description": "auth-core — bespoke magic-link auth Worker for skeptou.com and glossolalia.dev",
  "scripts": {
    "dev": "wrangler dev --env skeptou",
    "deploy:skeptou": "wrangler deploy --env skeptou",
    "deploy:glossolalia": "wrangler deploy --env glossolalia",
    "type-check": "tsc --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.0.0"
  }
}
